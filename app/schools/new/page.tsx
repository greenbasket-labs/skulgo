"use client"; import {FormEvent,useState} from "react"; import {useRouter} from "next/navigation";
export default function NewSchool(){const[m,setM]=useState("");const[b,setB]=useState(false);const r=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setB(true);const x=await fetch("/api/schools",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});const j=await x.json();setB(false);if(!x.ok){setM(j.error||"Could not create school");return;}r.push("/dashboard");r.refresh();}return <main className="shell"><div className="card" style={{maxWidth:600,margin:"40px auto"}}><p className="muted">SkulGo</p><h1>Register your school</h1><p className="muted">Your personal account becomes the school Admin account.</p><form onSubmit={submit} className="grid"><input required name="name" placeholder="School name"/><input required name="abbr" placeholder="School abbreviation"/><input required name="address" placeholder="Address"/><input required name="phone" placeholder="Phone"/><input required type="email" name="email" placeholder="School email"/><button className="button" disabled={b}>{b?"Creating…":"Create school"}</button></form>{m&&<p>{m}</p>}</div></main>;}